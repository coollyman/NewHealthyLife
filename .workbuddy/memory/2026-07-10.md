# 2026-07-10 工作日志

## 日志上传
- 事件编号: `6ab36e82b0d341e5a519a802bb1fb30d`
- 待定位问题

## 月历跨月查看完成情况
- 在 HomeStore 新增 `monthViewDate` 状态字段，打开月视图时初始化为 `selectedDate`
- 新增 `loadPreviousMonth()` / `loadNextMonth()` 方法，切换 `monthViewDate` 后重新加载月数据
- `loadMonthData` 现在以 `monthViewDate` 为基准计算网格，浏览月份不影响 `selectedDate`
- DateUtils 新增 `getMonthOffset(dateStr, monthOffset)` 工具函数
- MonthView 组件改用 `monthViewDate` 展示标题和网格，顶部加入上一月/下一月箭头（chevron_left/right）
- 完成状态视觉优化：全部完成=实心蓝色(10px)、部分完成=实心橙色(8px)、未完成=实心红色(8px)、无记录=空心圆环
- 新增颜色资源: status_all_done, status_partial_done, status_not_done, status_no_record（含 dark 主题）
- 新增测试文件 `MonthView.test.ets`，覆盖打开月视图、跨月导航、选择日期、关闭月视图等场景
- 构建验证通过（hvigorw.js + D 盘 DevEco Studio 路径）

## 月历 Review 修复 (P1/P2/P3)
- P1: `getMonthOffset` 月底溢出 bug — `Date.setMonth()` 在月底溢出（3/31 -1 → 3/3），改为先 setDate(1) 再 setMonth，最后 clamp 到目标月最大天数
- P1: 新增 7 个边界测试用例（Jan31、Mar31、Dec31、闰年 Feb29 等）
- P2: 新增 `isMonthLoading` 状态，`loadPreviousMonth`/`loadNextMonth` 串行化（加载中直接 return），MonthView 箭头加载时 opacity 降至 0.4
- P3: 删除 `loadMonthData` 中未使用的 `dayTaskList`/`dayTaskMap` 查询（死代码 + 多余 DB 查询）
- 构建验证通过

## 修复添加/编辑任务页标题栏布局
- 问题：AddTaskPage 和 EditTaskPage 的返回按钮和标题文字之前用 `layoutWeight(1) + textAlign(Center) + Blank(24)` 做伪居中，导致返回按钮和标题分开，不符合常规 app 布局
- 修复：改为返回按钮 + 标题紧挨在左上角，去掉 `layoutWeight`/`textAlign`/`Blank`，加 `margin({ left: 8 })` 间距，显式 `alignItems(VerticalAlign.Center)` 垂直居中
- 构建验证通过

## 个人信息展示与编辑功能
- 新增 `ProfileInfo` 模型 + `ProfileGender` 枚举（SECRET/MALE/FEMALE）：avatarUri, nickname, gender, birthDate(yyyy-MM-dd), heightCm, weightKg
- 新增 `IProfileRepository` 接口 + `ProfileRepository` 实现（Preferences 存储，单例，与 ConsecutiveDaysState 共用 healthyLifePrefs store）
- 新增 `ProfileStore` ViewModel（@Observed）：profile 字段 + init/saveProfile/reload
- 新增 `ProfileEditPage` 页面：标题栏(左上角)、头像选择(PhotoViewPicker IMAGE_TYPE maxSelectNumber=1)、昵称 TextInput、性别三选一 chip、出生日期 DatePicker、身高/体重 Number 输入、完成按钮
- 更新 `MinePage`：从 ProfileStore 展示头像和昵称，点击用户卡和"个人资料"菜单跳转编辑页
- 更新 `EntryAbility`：初始化 ProfileRepository + ProfileStore，存入 AppStorage
- 注册 `pages/ProfileEditPage` 到 `main_pages.json`
- 新增字符串资源：profile_avatar, profile_nickname, profile_gender, profile_gender_male/female/secret, profile_birth_date, profile_clear, profile_height, profile_weight, unit_cm, unit_kg
- 新增 `ProfileStore.test.ets`：默认值、保存读取、部分字段、覆盖、reload、deepClone 测试
- 新增 `FakeProfileRepo` 在 FakeRepositories.ets
- 构建验证通过（修复 DatePickerResult.month 可能 undefined 的编译错误）

## 创新功能 SPEC 实施（月历稳定化 + 目标建议 + 补救机制 + 成就与周复盘）
基于 `docs/superpowers/specs/2026-07-10-healthy-life-innovation-spec.md`，分 4 个 Phase 完整实施：

### Phase 1: 月历热力图稳定化
- 补齐 `updateMonthViewData` 方法：月视图打开时，checkin/enableTask/disableTask/updateTaskTarget 后同步更新对应格状态
- 3 个方法增加 `updateCalendarDayStatus` + `updateMonthViewData` 调用

### Phase 2: 智能目标调整
- 新建 `domain/rules/GoalSuggestionRules.ets`：纯函数 suggestGoalAdjustment
  - 连续5天完成→提高一档；7天<3天→降低一档；3-4天→保持；样本<3天→none；不越界
- HomeStore 增加 `getGoalSuggestion(taskId)` 方法：查最近7天 DayTaskInfo 调用规则
- 新建 `components/home/GoalSuggestionCard.ets`：建议文案 + 采用/暂不按钮
- EditTaskPage 集成建议卡片 + `acceptGoalSuggestion` 方法
- 新增 `GoalSuggestion.test.ets`：边界值全覆盖（increase/decrease/keep/none/最大最小不越界）

### Phase 3: 补救机制
- 新建 `domain/models/RecoveryInfo.ets`：date/weekKey/used/completionPercentAtUse/createdAt
- DbSchema 扩展 TABLE_RECOVERY + RECOVERY_COLUMNS，DatabaseHelper 自动建表
- 新建 `data/db/RecoveryRepository.ets` + `IRecoveryRepository` 接口
- 新建 `domain/rules/RecoveryRules.ets`：canUseRecovery/isDayCompletedForStreak/createRecoveryRecord/getWeekKey
- ConsecutiveDaysRules.onNewDay 增加 `yesterdayRecovery` 参数（默认 null，向后兼容）
- HomeStore 增加 `recoveryRepo`、`todayRecovery`、`canUseRecoveryToday()`、`useRecoveryToday()` 方法
  - 月视图 loadMonthData 增加补救状态(status=4)检查
  - getDayStatus 增加 70%+有补救→status=4 分支
- 新建 `components/home/RecoveryPrompt.ets`：首页轻量提示 + promptAction.showDialog 确认
- MonthView statusIndicator 增加 status=4 蓝色描边视觉
- HomePage 集成 RecoveryPrompt
- HomeStoreDeps 增加 recoveryRepo 参数；3 个现有测试文件 makeDeps 更新
- 新增 `FakeRecoveryRepo` 在 FakeRepositories.ets
- 新增 `Recovery.test.ets`：补救资格、连续天数不中断、weekKey、onNewDay 整合

### Phase 4: 成就与周复盘
- 新建 `domain/rules/AchievementRules.ets`：稳定完成/恢复能力/全勤周三类多维成就
- 新建 `domain/rules/WeeklyReviewRules.ets`：完成率、最稳定/最易遗漏任务、建议
  - 使用 `TaskCompletionStats` class 替代对象字面量（ArkTS 规则）
- HomeStore 增加 `getWeeklyReview()` 方法
- AchievementPage 升级：Scroll 包裹、周复盘卡片、多维成就展示
- 新增 `AchievementUpgrade.test.ets` + `WeeklyReview.test.ets`
- List.test.ets 注册 4 个新测试套件

### 构建验证
- 使用 `--no-daemon` 绕过 hvigor 守护进程 PATH 问题（restool DLL 找不到）
- 最终 BUILD SUCCESSFUL
- 所有新代码遵循现有分层（domain/data/services/viewmodel/pages/components）

### 新增文件清单
- domain/rules/GoalSuggestionRules.ets
- domain/rules/RecoveryRules.ets
- domain/rules/AchievementRules.ets
- domain/rules/WeeklyReviewRules.ets
- domain/models/RecoveryInfo.ets
- data/db/RecoveryRepository.ets
- components/home/GoalSuggestionCard.ets
- components/home/RecoveryPrompt.ets
- test/GoalSuggestion.test.ets
- test/Recovery.test.ets
- test/AchievementUpgrade.test.ets
- test/WeeklyReview.test.ets

## 修复用户等级 LV.7 写死问题
- 问题：MinePage 的"LV.7"直接用 `$r('app.string.user_level')`（值="LV.7"），写死不变
- 新建 `domain/rules/UserLevelRules.ets`：根据 maxConsecutiveDays 动态计算等级
  - 0-2天→LV.1，3-6→LV.2，7-29→LV.3，30-49→LV.4，50-72→LV.5，73-98→LV.6，99+→LV.7
  - 等级阶梯与 AppConstants.ACHIEVEMENT_THRESHOLDS 一致
  - `getUserLevelLabel(maxDays)` 返回 "LV.X" 文案
  - `getDaysToNextLevel(maxDays)` 返回到下一级还差多少天
- MinePage 改用 `@StorageLink('homeStore')` 获取连续天数，调用 `getUserLevelLabel` 动态展示
- 新增 `UserLevel.test.ets`：覆盖所有等级边界和 nextLevel 计算
- 构建通过

## 自定义习惯任务 MVP 闭环实施
从"7个预设任务启停"升级为"预设任务 + 用户自定义任务"共存。

### 模型与数据库
- TaskInfo 扩展 12 个新字段：taskName, iconKey, taskType, step, unitText, pickerType, targetOptions, scheduleDays, isPreset, isArchived, sortOrder, createdAt
- 新增 CustomTaskInput 类型 + CUSTOM_TASK_ID_START=100
- DbSchema TASK_COLUMNS 扩展 12 列 + TASK_MIGRATION_COLUMNS 用于 ALTER TABLE
- DatabaseHelper 新增 migrateTaskTable()：检测缺失列 → ALTER TABLE ADD COLUMN → 回填默认值
- TaskCatalog 每个预设项增加 iconKey 和 unitText；createDefaultTaskInfoList 填充完整元数据
- TaskInfoRepository 新增 createTask/archiveTask/getNextTaskId/getNextSortOrder + backfillTaskMetadata
- mapRow 增加 parseTaskType/parsePickerType 替代 `as any`（ArkTS 规则）

### 统一元数据解析
- 新建 `domain/constants/TaskDisplayMeta.ets`：
  - TaskDisplayMeta 类 + getTaskDisplayMeta(task) 统一解析（预设从 catalog，自定义从 TaskInfo 字段）
  - getIconResource(iconKey) + getDialogIconResource(iconKey) — 12 个内置图标白名单
  - ICON_KEY_OPTIONS 数组供自定义任务选择
  - isScheduledOnDay(task, dayOfWeek) — scheduleDays 空为每天

### 规则重构
- TaskRules.checkinTask 从 `(dayTask)` 改为 `(dayTask, taskInfo)` — 不再依赖 getTaskCatalogItem
- DayInitRules.initializeDayData + ensureDayTaskSnapshots 增加 scheduleDays 过滤
- ReminderService.buildReminderIntent 从 taskInfo 字段判断类型，不依赖 catalog

### HomeStore 扩展
- 新增 createCustomTask(input) / updateTaskConfig(task) / archiveCustomTask(taskId)
- ITaskRepository 接口增加 createTask/archiveTask/getNextTaskId/getNextSortOrder

### UI 改造
- AddTaskPage 重构：顶部"创建自定义习惯"入口 + 表单 + 下方预设列表
- EditTaskPage 增加删除按钮（仅自定义任务）
- TaskList/TaskClockDialog 改用 getTaskDisplayMeta
- FormService.getCardTaskName 增加 allTasks 参数支持自定义任务名称

### 测试
- 新增 CustomTask.test.ets
- 三个现有测试文件 checkinTask 调用更新为两参数版本
- 构建通过

### 新增文件
- domain/constants/TaskDisplayMeta.ets
- test/CustomTask.test.ets
